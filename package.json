{
  "name": "server",
  "version": "1.0.0",
  "main": "Server.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon Server.mjs",
    "start": "node Server.mjs",
    "heroku-postbuild": "npm install --prefix server/client && npm run build --prefix server/client"
  },
  "keywords": [],
  "author": "Julian Jimenez",
  "license": "ISC",
  "description": "The server receives and responds to client requests for our notebook software.",
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.4"
  }
}
